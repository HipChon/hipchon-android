<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/white"
        tools:context=".ui.onboard.view.SignupActivity">

        <com.google.android.material.appbar.MaterialToolbar
            android:id="@+id/mt_signup_title"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            app:layout_constraintTop_toTopOf="parent"
            app:navigationIcon="@drawable/ic_chevron_left" />

        <androidx.fragment.app.FragmentContainerView
            android:id="@+id/fcv_signup"
            android:name="com.gritbus.hipchon.ui.onboard.view.SignupTermsFragment"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintTop_toBottomOf="@id/mt_signup_title" />
    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>
